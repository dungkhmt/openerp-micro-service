import { Box } from "@mui/material";
import { GeneralManager } from "./GeneralManager";

const ProjectViewSetting = () => {
  return (
    <Box sx={{ width: "75%" }}>
      <GeneralManager />
    </Box>
  );
};

export { ProjectViewSetting };
